#ifndef MAIN_H_
#define MAIN_H_

/**
 * Program-wide dependencies
 */
#include <stdio.h>      // for I/O
#include <stdlib.h>     // for EXIT_SUCCESS, EXIT_FAILURE, etc.
#include <string.h>     // for string manipulation

/**
 * Global constants
 */
int _DEBUG = 0;         // Set to 1 to enable debug mode
int _MAX_INPUT = 256;   // Maximum input length in characters

#endif
